top-level-division: chapter
metadata:
  documentclass: report
  fontsize: 11pt
  geometry: letterpaper, margin=0.5in
  microtypeoptions: tracking = true, letterspace = 500
  # header-includes:
  #   - '`\renewcommand{\maketitle}{}`{=latex}'
# ! --include-in-header overwrites header-includes:
include-in-header:
  - ${USERDATA}/includes/old-resume-header.latex
